import {SortDirectionPipe} from './sort-direction.pipe';

describe('SortDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
