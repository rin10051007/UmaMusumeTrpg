import {SysPermissionPipe} from './sys-permission.pipe';

describe('SysPermissionPipe', () => {
  it('create an instance', () => {
    const pipe = new SysPermissionPipe();
    expect(pipe).toBeTruthy();
  });
});
